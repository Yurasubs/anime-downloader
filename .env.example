# WebSocket host for the multi-downloader-nx backend server
# Default: uses the current browser host (same origin)
# For development, point to the backend port:
NEXT_PUBLIC_WS_HOST=localhost:3000
CONFIG_DIR=/config/